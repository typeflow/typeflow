input user: { id: number, firstName: string, lastName: string, tags: string[] }

use slug(v: string): string from "./helpers"

map {
  id: user.id,
  fullName: user.firstName + " " + user.lastName,
  slugName: slug(user.firstName + " " + user.lastName),
  tagCount: count(user.tags),
}
